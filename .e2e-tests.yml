.skip_on_watch: &skip_on_watch
  rules:
    - if: $WATCH
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - when: on_success

.e2e-test-template:
  tags:
    - all
  image:
    name: ${CI_REGISTRY}/${CI_PROJECT_PATH}/aval:${CI_COMMIT_REF_NAME}
    pull_policy: always
  <<: *skip_on_watch
  variables:
    TARGET_BUILD_TYPE: "release"
    AVAL_VERBOSE: "true"
    GIT_STRATEGY: none
  script:
    - /usr/local/bin/entrypoint.sh "docker run -e SOC_UDT=$SOC_UDT --privileged --pid host
      -v /var/run/docker.sock:/var/run/docker.sock -v /home/torizon:/home/torizon
      ${CI_REGISTRY}/${CI_PROJECT_PATH}/aval-e2e-tests:${CI_COMMIT_REF_NAME} /suites/run-tests.sh"
      --remove-databases --copy-artifact /home/torizon/report.xml report.xml
      --delegation-config $DELEGATION_PATH --before "docker system prune --force --all"
      ${AVAL_EXTRA_OPTIONS} --run-before-on-host "/aval/host_command.sh device_information.json"
  artifacts:
    when: always
    reports:
      junit: report.xml
  needs:
    - e2e-tests-container-build-manifest
    - unit-test

e2e-test-apalis-imx8qm-release-with-rac:
  extends: .e2e-test-template
  resource_group: apalis-imx8qm
  variables:
    SOC_UDT: "apalis-imx8qm"
    USE_RAC: "true"
    DELEGATION_PATH: "/aval/delegation_config.toml"
    AVAL_EXTRA_OPTIONS: "--ignore-different-secondaries-between-updates"

e2e-test-apalis-imx8qm-nightly:
  extends: .e2e-test-template
  resource_group: apalis-imx8qm
  variables:
    SOC_UDT: "apalis-imx8qm"
    TARGET_BUILD_TYPE: "nightly"
    DELEGATION_PATH: "/aval/delegation_config.toml"
    AVAL_EXTRA_OPTIONS: "--ignore-different-secondaries-between-updates"
  needs:
    - e2e-test-apalis-imx8qm-release-with-rac

e2e-test-apalis-imx8qm-release-again:
  extends: .e2e-test-template
  resource_group: apalis-imx8qm
  variables:
    SOC_UDT: "apalis-imx8qm"
    TARGET_BUILD_TYPE: "release"
    DELEGATION_PATH: "/aval/delegation_config.toml"
    AVAL_EXTRA_OPTIONS: "--ignore-different-secondaries-between-updates"
  needs:
    - e2e-test-apalis-imx8qm-nightly

e2e-test-verdin-imx8mpq-npu-release:
  resource_group: verdin-imx8mpq
  tags:
    - all
  image:
    name: ${CI_REGISTRY}/${CI_PROJECT_PATH}/aval:${CI_COMMIT_REF_NAME}
    pull_policy: always
  <<: *skip_on_watch
  variables:
    TARGET_BUILD_TYPE: "release"
    AVAL_VERBOSE: "true"
    AVAL_EXTRA_OPTIONS: "--ignore-different-secondaries-between-updates"
  script:
    - /usr/local/bin/entrypoint.sh "docker run -e SOC_UDT=$SOC_UDT --privileged --pid host
      -v /var/run/docker.sock:/var/run/docker.sock -v /home/torizon:/home/torizon
      ${CI_REGISTRY}/${CI_PROJECT_PATH}/aval-e2e-tests:${CI_COMMIT_REF_NAME} /suites/run-tests.sh"
      --remove-databases --copy-artifact /home/torizon/report.xml report.xml
      --delegation-config /aval/delegation_config.toml --device-config /aval/verdin-imx8mpq-npu.toml
      ${AVAL_EXTRA_OPTIONS} --before "docker system prune --force --all"
  artifacts:
    when: always
    reports:
      junit: report.xml
  needs:
    - e2e-tests-container-build-manifest
    - unit-test

e2e-test-sk-am62-nightly:
  extends: .e2e-test-template
  resource_group: sk-am62
  variables:
    TARGET_BUILD_TYPE: "nightly"
    SOC_UDT: "sk-am62"
    DELEGATION_PATH: "/aval/delegation_config_common.toml"
    AVAL_EXTRA_OPTIONS: ""

e2e-test-torizon-x86-nightly:
  extends: .e2e-test-template
  resource_group: torizon-x86
  variables:
    TARGET_BUILD_TYPE: "nightly"
    SOC_UDT: "torizon-x86"
    DELEGATION_PATH: "/aval/delegation_config_common.toml"
    AVAL_EXTRA_OPTIONS: ""

e2e-test-any-arm64-cd:
  extends: .e2e-test-template
  variables:
    TARGET_BUILD_TYPE: "nightly"
    SOC_UDT: "arm64"
    USE_COMMON_DEVICES: "true"
    DELEGATION_PATH: "/aval/delegation_config_common.toml"
    AVAL_EXTRA_OPTIONS: ""

e2e-test-any-arm64-nightly:
  extends: .e2e-test-template
  variables:
    TARGET_BUILD_TYPE: "nightly"
    SOC_UDT: "arm64"
    DELEGATION_PATH: "/aval/delegation_config.toml"
    AVAL_EXTRA_OPTIONS: "--ignore-different-secondaries-between-updates"

e2e-test-any-arm-release:
  extends: .e2e-test-template
  variables:
    TARGET_BUILD_TYPE: "release"
    SOC_UDT: "arm"
    DELEGATION_PATH: "/aval/delegation_config.toml"
    AVAL_EXTRA_OPTIONS: "--ignore-different-secondaries-between-updates"

e2e-test-any-arm64-shell:
  tags:
    - shell
  <<: *skip_on_watch
  variables:
    TARGET_BUILD_TYPE: "nightly"
    SOC_UDT: "arm64"
    AVAL_VERBOSE: "true"
    AVAL_EXTRA_OPTIONS: "--ignore-different-secondaries-between-updates"
  needs:
    - e2e-tests-container-build-manifest
    - unit-test
  before_script:
    - sudo apt update && sudo apt upgrade -y
    - sudo apt install -y python3 python3-venv make
    - python3 --version
    - ./aws_database/install-ssm.sh
  script:
    - python3 -m venv venv
    - source venv/bin/activate
    - pip install --upgrade pip
    - make install
    - python main.py "docker run -e SOC_UDT=$SOC_UDT --privileged --pid host
      -v /var/run/docker.sock:/var/run/docker.sock -v /home/torizon:/home/torizon
      ${CI_REGISTRY}/${CI_PROJECT_PATH}/aval-e2e-tests:${CI_COMMIT_REF_NAME} /suites/run-tests.sh"
      --remove-databases --copy-artifact /home/torizon/report.xml report.xml
      --delegation-config ./delegation_config.toml --before "docker system prune --force --all"
      ${AVAL_EXTRA_OPTIONS} --run-before-on-host "./host_command.sh device_information.json"
  artifacts:
    when: always
    reports:
      junit: ./report.xml

e2e-test-any-arm64-powershell:
  tags:
    - pwsh
  <<: *skip_on_watch
  variables:
    TARGET_BUILD_TYPE: "nightly"
    SOC_UDT: "arm64"
    AVAL_VERBOSE: "true"
    AVAL_EXTRA_OPTIONS: "--ignore-different-secondaries-between-updates"
  needs:
    - e2e-tests-container-build-manifest
    - unit-test-windows
  before_script:
    # If Python is already installed, winget returns an error, which causes the CI job to fail.
    # So, check if it's installed and, if it is, force LASTEXITCODE = 0.
    - |
      try {
        winget install --id Python.Python.3.11 --accept-source-agreements --accept-package-agreements
      } catch {
        Write-Output "Ignoring error from 'winget install --id Python.Python.3.11': $($_.Exception.Message)"
      }
      $global:LASTEXITCODE = 0
    - python --version
    - .\aws_database\install-ssm.ps1
  script:
    - Write-Output "Setting up environment for Windows runner..."
    - python -m venv venv
    - .\venv\Scripts\activate
    - pip install -r requirements.txt
    - python main.py "docker run -e SOC_UDT=$env:SOC_UDT --privileged
      --pid host -v /var/run/docker.sock:/var/run/docker.sock -v /home/torizon:/home/torizon
      $env:CI_REGISTRY/$env:CI_PROJECT_PATH/aval-e2e-tests:$env:CI_COMMIT_REF_NAME /suites/run-tests.sh"
      --remove-databases --copy-artifact /home/torizon/report.xml report.xml
      --delegation-config .\delegation_config.toml --before "docker system prune --force --all"
      --run-before-on-host ".\host_command.ps1 device_information.json"
  artifacts:
    when: always
    reports:
      junit: .\report.xml
